# Core data / numerics
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# Market data
yfinance>=0.2.40
pyarrow>=14.0            # parquet cache

# Technical indicators
pandas-ta>=0.4.71b      # 0.4.71b0+ is numpy-2.x compatible; older builds
                        # break on `from numpy import NaN` — upgrade if you see that

# Charting (agent's visual input)
mplfinance>=0.12.10b

# LLM agent (real mode only)
openai>=1.40            # GPT-4o function calling
# anthropic>=0.34       # uncomment to use Claude instead
